name: deploy dev manual
on:
  workflow_dispatch:
    inputs:
      image-tag:
        description: 'Image tag to deploy to dev'
        required: true

jobs:
  deploy:
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        cluster: [ dev-gcp ]
        app: [ bruker-api, produsent-api, kafka-reaper, statistikk, ekstern-varsling ]
    steps:
      - uses: actions/checkout@v1
      - uses: nais/deploy/actions/deploy@v1
        name: "${{ matrix.cluster }}: deploy ${{ matrix.app }}"
        env:
          IMAGE: ghcr.io/${{ github.repository }}/arbeidsgiver-notifikasjon-produsent-api:${{ github.event.inputs.image-tag }}
          APIKEY: ${{ secrets.NAIS_DEPLOY_APIKEY }}
          CLUSTER: ${{ matrix.cluster }}
          RESOURCE: config/${{ matrix.cluster }}-${{ matrix.app }}.yaml